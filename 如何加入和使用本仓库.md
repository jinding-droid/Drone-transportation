# 如何加入、查看和使用本仓库

## 一、加入仓库

### 作为协作者加入

1. 把你的 GitHub 用户名发给仓库管理员。
2. 管理员进入仓库的 `Settings` -> `Collaborators`，点击 `Add people`，输入你的用户名并发送邀请。
3. 在 GitHub 通知或注册邮箱中接受邀请。
4. 接受后即可查看、编辑并上传仓库文件。

### 只查看或复制一份

公开仓库可以直接打开仓库网址查看。点击 `Fork` 可以在自己的账号下复制一份；私有仓库必须先获得访问权限。

## 二、查看和下载

1. 打开仓库网址，首页可以看到 `README.md`。
2. 点击 `数据` 等文件夹进入目录，再点击文件名查看或下载。
3. 下载单个文件时，打开文件后点击下载按钮。
4. 下载全部资料时，点击绿色 `Code` -> `Download ZIP`。

## 三、使用 Git 克隆

先安装 Git，在仓库页面点击绿色 `Code`，复制 HTTPS 地址，然后执行：

```powershell
git clone https://github.com/用户名/仓库名.git
cd 仓库名
```

第一次提交前设置身份：

```powershell
git config --global user.name "你的 GitHub 昵称"
git config --global user.email "你的 GitHub 邮箱"
```

## 四、编辑和上传

网页适合修改 README 和说明文字：打开文件，点击铅笔图标，修改后在页面底部填写提交说明并点击 `Commit changes`。

Word、Excel、PDF、MAT、TIF 等文件建议使用 `Add file` -> `Upload files` 上传新版本，或使用 Git：

```powershell
git pull
git status
git add "文件名"
git commit -m "说明本次修改"
git push
```

上传整个目录的改动可以使用：

```powershell
git add .
git commit -m "更新 D 题数据"
git push
```

多人协作时先执行 `git pull`。出现冲突时，保留正确内容后再次执行 `git add`、`git commit` 和 `git push`。

## 五、注意事项

- GitHub 单个文件超过 100 MB 不能直接上传；大型数据应使用 Git LFS 或其他存储服务。
- 原始数据不要覆盖，处理后的表格、地图或模型结果放入单独目录。
- 上传前检查 Excel、PDF、MAT、TIF 和 HTML 文件是否完整可用。
- 不要上传密码、密钥、身份证号、手机号等隐私信息。

## 六、常用命令

```powershell
git clone 仓库地址
git pull
git status
git add .
git commit -m "修改说明"
git push
```