# 如何加入、查看和使用本仓库

## 一、加入仓库

### 方式一：作为协作者加入（推荐）

1. 把你的 GitHub 用户名发给仓库管理员。
2. 管理员进入仓库的 `Settings` -> `Collaborators`，点击 `Add people`，输入你的用户名并发送邀请。
3. 你在 GitHub 右上角通知或注册邮箱中接受邀请。
4. 接受后，你就可以查看、编辑并上传仓库文件。

### 方式二：只查看或复制一份

公开仓库可以直接打开仓库网址查看。点击右上角 `Fork` 可以在自己的账号下复制一份；如果仓库是私有的，则必须先获得访问权限。

## 二、查看仓库

1. 打开仓库网址，首页可以看到 `README.md`。
2. 点击文件夹名称进入附件目录，点击文件名查看内容或下载。
3. 下载单个文件时，打开文件后点击右上角下载按钮。
4. 下载整个仓库时，点击绿色 `Code` 按钮，选择 `Download ZIP`。

## 三、使用 Git 克隆到电脑（适合长期编辑）

先安装 Git，然后在仓库页面点击绿色 `Code`，复制 HTTPS 地址，在终端执行：

```powershell
git clone https://github.com/用户名/仓库名.git
cd 仓库名
```

第一次提交前设置身份：

```powershell
git config --global user.name "你的 GitHub 昵称"
git config --global user.email "你的 GitHub 邮箱"
```

## 四、编辑并上传文件

### 网页直接编辑

1. 打开要修改的文本文件，点击铅笔图标。
2. 修改内容后滚动到页面底部，在提交框填写说明。
3. 选择 `Commit changes` 完成上传。

网页适合修改 README、说明文字和小型文本文件。Word、Excel 等二进制文件建议使用 Git 或网页的 `Add file` -> `Upload files` 上传新版本。

### 使用 Git 上传

在仓库本地目录执行：

```powershell
git pull
git status
git add "文件名"
git commit -m "说明本次修改"
git push
```

上传整个目录中的改动可以使用：

```powershell
git add .
git commit -m "更新题目附件"
git push
```

如果多人同时修改，先执行 `git pull`。出现冲突时，保留正确内容后再次执行 `git add`、`git commit` 和 `git push`。

## 五、文件和安全注意事项

- GitHub 普通仓库单个文件建议小于 50 MB，单个文件超过 100 MB 不能直接上传；大型数据应使用 Git LFS 或网盘。
- 不要上传密码、密钥、身份证号、手机号等隐私信息。
- 原始题目和原始附件尽量保持不改动；修改后的表格另存为新文件。
- 文件名尽量使用清晰、稳定的名称，避免频繁改名导致链接失效。
- 上传前检查 Excel 是否能正常打开、Word 是否包含个人信息。

## 六、常用命令速查

```powershell
git clone 仓库地址       # 第一次下载
git pull                 # 获取别人最新提交
git status               # 查看本地改动
git add .                # 暂存改动
git commit -m "修改说明" # 创建本地提交
git push                 # 上传到 GitHub
```