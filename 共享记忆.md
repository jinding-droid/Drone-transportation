# 协作者共享记忆

本文件用于记录协作者在本仓库中的实际工作内容、关键决定和交接信息。提交到 GitHub 时，除了在提交说明中概括本次修改，还必须同步更新本文件，确保后来加入的协作者能够快速了解项目进展。

## 使用规则

1. 开始工作前先执行 `git pull`，阅读本文件最近的记录，并确认自己负责的任务没有和他人重复。
2. 每完成一个明确任务，就在“工作记录”中新增一条记录。不要只写“已修改”“已更新”等无法复现工作内容的描述。
3. 每条记录至少写清：日期、协作者、任务、具体修改、涉及文件、验证方式、当前状态和后续事项。
4. 提交说明要和本文件中的记录对应。例如：`补充共享记忆协作规范`。提交说明用于快速浏览，本文件用于完整交接。
5. 不覆盖原始题目和原始数据。处理脚本、结果和中间文件应放在合适的独立目录，并在记录中说明输入、输出和用途。
6. 不记录密码、令牌、个人隐私或其他敏感信息；如需说明配置，只记录变量名、文件位置和脱敏后的示例。
7. 推送前检查 `git diff`、`git status` 和待提交文件，避免把无关文件、临时文件或大型未压缩文件上传到仓库。
8. 若发生冲突，先保留双方有效记录，再合并内容；不要删除他人的工作记录来解决冲突。

## 工作记录

按时间倒序新增记录，最新工作放在最前面。记录完成后，将状态更新为“已完成”；未完成任务应明确阻塞原因。

### 记录模板

```markdown
### YYYY-MM-DD：任务标题

- 协作者：姓名或 GitHub 用户名
- 任务：本次要解决的问题
- 具体修改：完成了哪些分析、代码、数据处理或文档工作
- 涉及文件：列出新增、修改或删除的文件
- 验证方式：运行的命令、检查步骤或得到的结果
- 当前状态：进行中 / 已完成 / 已阻塞
- 后续事项：下一位协作者需要继续做什么；没有则写“无”
```

### 2026-09-24：建立协作者共享记忆

- 协作者：仓库协作者
- 任务：建立统一的协作记忆规范，补充提交说明之外的工作交接记录
- 具体修改：新增本文件，规定工作记录格式、提交前检查、原始数据保护、冲突处理和交接要求
- 涉及文件：`共享记忆.md`
- 验证方式：检查文件已位于仓库根目录，确认包含记录模板和首条建立记录；已创建本地提交 `b639e51`
- 当前状态：已完成；推送暂被 GitHub 权限拒绝
- 后续事项：使用具有 `jinding-droid/Drone-transportation` 写权限的 GitHub 账号认证后，执行 `git push origin main`

### 2026-09-24：部署 HWB 数学建模 Skills

- 协作者：仓库协作者
- 任务：学习并部署 [hwb-math-modeling-skills](https://github.com/xieyouxixi/hwb-math-modeling-skills)，用于本题的题意分析、建模选型和论文自查
- 具体修改：阅读外部仓库 README、总控工作流、题意翻译、建模思路、模型适配和论文评审规则；将 16 个不重复的 `hwb-*` Skill 部署到 Windows 用户级 Codex 目录 `%USERPROFILE%\.codex\skills\`
- 部署范围：总控工作流、题意翻译、建模思路、问题重述、模型字典、摘要/问题分析/模型假设/符号/模型求解/参考文献附录自查、AIGC 审计、格式检查、综合评审和奖项定位
- 同名处理：同名 Skill 只安装一份；`hwb-review-paper` 选择综合评审版本，`hwb-problem-restatement` 和 `hwb-ai-usage-disclosure` 选择生成类版本
- 使用原则：模型建议必须以真实题目、数据和验证证据为基础，不虚构结果；论文结论需人工复核；遵守竞赛匿名、学术诚信和赛题保密要求
- 涉及文件：仅修改本文件；外部 Skill 文件位于用户级 Codex 目录，不复制到本仓库，也不提交到 GitHub
- 验证方式：确认 16 个部署目录均存在 `SKILL.md`
- 当前状态：已完成
- 后续事项：处理本题时优先从 `hwb-modeling-workflow` 进入，再按阶段调用专项 Skill；每次实际使用的 Skill 和产出继续记录在本文件

## 当前任务看板

| 任务 | 负责人 | 状态 | 说明 |
| --- | --- | --- | --- |
| 建立协作者共享记忆规范 | 仓库协作者 | 已完成 | 本文件已创建，后续工作按模板记录 |

## 提交前检查清单

- [ ] 已执行 `git pull` 并阅读最新共享记忆
- [ ] 已在本文件新增或更新对应工作记录
- [ ] 已说明具体修改、涉及文件和验证结果
- [ ] 已确认没有覆盖原始题目或原始数据
- [ ] 已检查没有敏感信息、临时文件或无关改动
- [ ] 提交说明能概括本次实际修改
- [ ] 已执行 `git status` 和 `git diff --check`
- [ ] 已成功执行 `git push`，或已记录推送失败原因